# Local Chat Model UI – environment variables
# Copy to .env and adjust as needed.

# Tools server port (default: 3001)
PORT=3001

# Optional shared secret for tool endpoints. When set, clients must send x-tool-api-key.
# Frontend: set VITE_TOOL_API_KEY to match.
TOOL_API_KEY=

# Enables /api/tools/brave_search. Get a key at https://brave.com/search/api/
BRAVE_API_KEY=

# Comma-separated allowed CORS origins (default includes localhost:5173, 5174)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5174

# Frontend only: must match TOOL_API_KEY when server requires auth
# VITE_TOOL_API_KEY=
